
import { Tournament } from '@/backend/tournament_backend'
import React from 'react'

type Props = {
  tournament: Tournament
}

const TournamentComponent = (props: Props) => {

  return (
    <div>

    </div>
  )
}

export default TournamentComponent
